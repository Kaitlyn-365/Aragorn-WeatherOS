// Optional texture payload. Run `python3 embed_texture.py` with world.jpg present
// to replace this placeholder with a base64 data URL for file:// usage.
window.WORLD_JPG_DATA_URL = window.WORLD_JPG_DATA_URL || null;
